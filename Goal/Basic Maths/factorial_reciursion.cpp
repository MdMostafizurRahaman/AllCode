#include<bits/stdc++.h>
using namespace std;

vector<long long >factorialNumbers(long long N){
    
}

int main(){
    long long N;
    cin >> N;
    vector<long long> ans = factorialNumbers(N);
}
